class MapConfig:
    def __init__(self, hp, nb_enemies, delay_enemy, nb_per_spawn, shoot_delay):
        self.hp = hp
        self.nb_enemies = nb_enemies
        self.shoot_delay = shoot_delay
        self.delay_enemy = delay_enemy
        self.nb_per_spawn = nb_per_spawn
